  <%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group input-group">
    <%= f.label :name %>
    <%= f.text_field :name, autofocus: true, placeholder: "Enter Name", class: "form-control" %>
  </div>

  <div class="form-group input-group">
    <%= f.label :description %>
    <%= f.text_area :description, autofocus: true, placeholder: "Enter Item Description", class: "form-control" %>
  </div>

  <div class="form-group input-group">
    <%= f.label :price %>
    <%= f.text_field :price, autofocus: true, placeholder: "Enter Item Price", class: "form-control" %>
  </div>

    <%= f.submit "Create", class: 'btn btn-primary' %>
<% end %>

